# Backend Requirements
# FastAPI and Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
motor==3.3.2
pymongo==4.6.1

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

# Validation
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0

# LLM
groq==0.4.2

# NLP and ML (lighter versions for deployment)
# Pin spacy to version with pre-built wheels
spacy==3.7.4
# Pre-built blis wheel
blis>=0.7.9,<1.0.0
scikit-learn==1.4.0
# Use CPU-only torch for smaller deployment
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
sentence-transformers==2.2.2

# Template Engine
Jinja2==3.1.3

# Background Tasks (optional - comment out if not using Redis)
# celery[redis]==5.3.6
# redis==5.0.1

# Utilities
python-dotenv==1.0.0
bleach==6.1.0
httpx==0.26.0

# Logging
structlog==24.1.0
